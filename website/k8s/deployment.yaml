apiVersion: apps/v1
kind: Deployment
metadata:
  name: clawtab-frontend
  namespace: clawtab
  labels:
    app: clawtab-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clawtab-frontend
  template:
    metadata:
      labels:
        app: clawtab-frontend
    spec:
      containers:
        - name: clawtab-frontend
          image: registry.tonisives.com/clawtab-frontend
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
      imagePullSecrets:
        - name: tonisives-registry-secret
---
apiVersion: v1
kind: Service
metadata:
  name: clawtab-frontend
  namespace: clawtab
  labels:
    app: clawtab-frontend
spec:
  type: ClusterIP
  selector:
    app: clawtab-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
